package {{package}};

import java.io.IOException;
import java.util.List;

import com.google.gson.JsonObject;

import it.mcsquared.engine.web.controller.data.TableCounts;

public interface {{modelClassName}}Adapter<T> {

	List<JsonObject> search({{searchRequestArguments}});

	TableCounts count({{searchRequestArguments}});

	void save(T instance);

	RestRequest get(String id);

}
